Build me a full-stack Netflix-style movie recommendation web app using the Remix framework.

🧱 Architecture:
Use Remix (with React) for both frontend and backend logic (routing, API handling, loaders, and actions).

Integrate with an external Python microservice (Flask or FastAPI) to serve personalized movie recommendations based on user quiz data.

Use TMDb API to fetch real-time movie data (titles, genres, posters, etc.).

Use Supabase or Firebase for user authentication, user metadata storage, and persistent data such as watchlists and preferences.

🌐 Frontend (Remix + Tailwind or styled-components):
Create a modern, responsive UI that mimics Netflix's design:

Movie posters in horizontal sliders by genre/category

Featured movies banner

Dark/light theme toggle

Search bar with live filtering

Fully working routes/pages:

/ → Home (Recommended Movies)

/search → Search Page

/movie/:id → Movie Details Page

/quiz → First-time onboarding quiz (genres, languages, actors, etc.)

/profile → User preferences and saved movies

/settings → Account settings, dark mode, logout

🔐 Auth & Persistence:
Implement login/signup flow using Supabase or Firebase Auth (email + password).

Store:

User quiz responses

Saved or watched movies

Session tokens for persistent login

Show different UI for logged-in vs logged-out users.

🧠 Recommendation Engine (External Python API):
Build a Python-based content-based recommendation system (using pandas, sklearn, etc.).

Host it as an external API (on Render, Hugging Face, or Replit).

Remix will send POST requests with quiz/user data and receive personalized movie recommendations (list of TMDb IDs).

Show recommended movies on the homepage and update them if quiz data changes.

🔄 Integration:
Fetch TMDb movie details server-side in Remix loaders (use .env for API key).

Remix loaders and actions should:

Handle quiz submission and pass to Python API

Fetch data from Supabase/Firebase for user-specific content

Render server-side where appropriate

📦 Stack:
Frontend: React + Remix + Tailwind CSS (or styled-components)

Backend: Remix loaders/actions + external Python service

Auth & DB: Supabase (preferred) or Firebase

Movie API: TMDb

🧪 Features to Include:
First-time quiz on signup to initialize preferences

Live search with real-time results from TMDb

Save to Watchlist / "My List" functionality

Profile page with previously watched and saved movies

Full settings page (theme, account, logout)

Responsive for desktop/tablet/mobile

Persistent login and dynamic, personalized content

